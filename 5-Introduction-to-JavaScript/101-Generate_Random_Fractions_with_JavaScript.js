/*In this lesson we learn about Random Fraction Generator functions, and how to
utilize them with a loop.*/
function randomFraction() {

  // Only change code below this line.
var result = 0;
while (result === 0) {
    result = Math.random();
}
  return result;

  // Only change code above this line.
}
