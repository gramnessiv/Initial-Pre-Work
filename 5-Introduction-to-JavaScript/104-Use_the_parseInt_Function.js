/*In this lesson we learned how to use the ParseInt function, and how it can turn
a string into a interger.*/
function convertToInteger(str) {
  var a = parseInt(str);
  return a;
}

convertToInteger("56");
