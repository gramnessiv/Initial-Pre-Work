/*In this lesson we learn about the Equality Operator (==), and how it is used to
compare to values together, even a string to a number.*/
// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(12);
