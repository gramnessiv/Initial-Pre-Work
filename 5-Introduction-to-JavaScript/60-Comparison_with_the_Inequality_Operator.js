/*In this lesson we learn about the Inequality Operator (!=), and how it will also
convert data types for comparison.*/
// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(99);
