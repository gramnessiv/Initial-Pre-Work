/*In this lesson we learned about simplifying code by using boolean values to return
data previously returned by an else/if statement.*/
function isLess(a, b) {
  // Fix this code
  return a <= b;
}

// Change these values to test
isLess(10, 15);
