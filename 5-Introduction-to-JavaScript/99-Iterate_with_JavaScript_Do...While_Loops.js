/*In this lesson we learn about Do...While Loops, and how they will run the "Do"
code first, then it will run the "While" code until the condition returns false. */
// Setup
var myArray = [];
var i = 10;

// Only change code below this line.
do {
  myArray.push(i);
  i++;
  } while (i < 5);
